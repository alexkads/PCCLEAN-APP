import { ProgressIndicator, VerticalBox } from "std-widgets.slint";

// Stats Card Component usando Material Design
export component StatsCard inherits VerticalBox {
    in property <bool> has-results: false;
    in property <bool> is-scanning: false;
    in property <string> total-size-display: "0.00 GB";
    in property <int> total-items: 0;
    
    // Acessibilidade
    accessible-role: text;
    accessible-label: is-scanning ? "Scanning system" : 
                     has-results ? "Found " + total-size-display + " in " + total-items + " files" :
                     "Welcome to PCCLEAN";
    
    padding: 32px;
    spacing: 16px;
    alignment: center;
    min-height: 200px;
    
    // Estado inicial - Bem-vindo
    if !has-results && !is-scanning: VerticalBox {
        spacing: 12px;
        alignment: center;
        
        Text {
            text: "👋";
            font-size: 48px;
            horizontal-alignment: center;
        }
        
        Text {
            text: "Welcome to PCCLEAN";
            font-size: 24px;
            font-weight: 600;
            horizontal-alignment: center;
            vertical-alignment: center;
        }
        
        Text {
            text: "Click 'Start Scan' to analyze your system";
            font-size: 14px;
            horizontal-alignment: center;
            vertical-alignment: center;
            opacity: 0.8;
        }
    }
    
    // Estado de scanning
    if is-scanning: VerticalBox {
        spacing: 16px;
        alignment: center;
        
        ProgressIndicator {
            indeterminate: true;
            width: 100px;
        }
        
        Text {
            text: "Scanning your system...";
            font-size: 16px;
            horizontal-alignment: center;
            vertical-alignment: center;
        }
    }
    
    // Estado com resultados
    if has-results && !is-scanning: VerticalBox {
        spacing: 12px;
        alignment: center;
        
        Text {
            text: total-size-display;
            font-size: 56px;
            font-weight: 700;
            horizontal-alignment: center;
            vertical-alignment: center;
        }
        
        Text {
            text: total-items + " files detected";
            font-size: 14px;
            horizontal-alignment: center;
            vertical-alignment: center;
            opacity: 0.8;
        }
    }
}
